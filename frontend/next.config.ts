import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the Next.js dev mode N indicator in the corner — we have our own HUD.
  devIndicators: false,
};

export default nextConfig;
